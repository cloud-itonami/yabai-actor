;; yabai phishing-watch maturity — GENERATED by src/yabai/methods/maturity.cljc
;; DO NOT EDIT BY HAND. Deterministic: no network, no LLM, no clock.
;; score 461/1000
;;
;; Raising this number requires COVERAGE (more brands, more CT shards, more
;; corroborated observations). Loosening a threshold to raise detection counts
;; breaks a floor and drives the score to zero instead.

#:maturity{:score 461, :valid true, :dimensions {:brand-coverage 0.5, :log-coverage 0.5, :issuance-coverage 0.018, :observation-volume 0.536, :infra-breadth 1.0, :signal-independence 0.718}, :weights {:brand-coverage 0.25, :log-coverage 0.2, :issuance-coverage 0.25, :observation-volume 0.1, :infra-breadth 0.12, :signal-independence 0.08}, :targets {:brands 50, :logs 12, :observations 2000, :asns 60}, :floors {:benign-claims [], :missed-impersonations [], :ok true}}
