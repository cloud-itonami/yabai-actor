;; yabai phishing-watch maturity — GENERATED by src/yabai/methods/maturity.cljc
;; DO NOT EDIT BY HAND. Deterministic: no network, no LLM, no clock.
;; score 343/1000
;;
;; Raising this number requires COVERAGE (more brands, more CT shards, more
;; corroborated observations). Loosening a threshold to raise detection counts
;; breaks a floor and drives the score to zero instead.

#:maturity{:score 343, :valid true, :dimensions {:brand-coverage 0.5, :log-coverage 0.167, :observation-volume 0.132, :infra-breadth 0.45, :signal-independence 0.58}, :weights {:brand-coverage 0.3, :log-coverage 0.25, :observation-volume 0.2, :infra-breadth 0.15, :signal-independence 0.1}, :targets {:brands 50, :logs 6, :observations 2000, :asns 60}, :floors {:benign-claims [], :missed-impersonations [], :ok true}}
